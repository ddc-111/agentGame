server:
  port: 8080
  mode: debug

database:
  # 使用SQLite（默认）
  driver: sqlite
  dsn: game.db
  
  # 使用MySQL（取消注释以下配置）
  # driver: mysql
  # host: localhost
  # port: 3306
  # user: root
  # password: your_password
  # dbname: agentgame

ai:
  provider: openai
  api_key: your-api-key-here
  model: gpt-4

game:
  max_players: 100
  tick_rate: 20
